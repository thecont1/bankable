[project]
name = "bankable"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "marker-pdf>=1.10.2",
    "pandas>=2.0",
]
